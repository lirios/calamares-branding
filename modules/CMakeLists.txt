install(DIRECTORY "prepare/" DESTINATION "${INSTALL_LIBDIR}/calamares/modules/prepare")
install(DIRECTORY "ostree/" DESTINATION "${INSTALL_LIBDIR}/calamares/modules/ostree")
install(DIRECTORY "ostree-finalize/" DESTINATION "${INSTALL_LIBDIR}/calamares/modules/ostree-finalize")
